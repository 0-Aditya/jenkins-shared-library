# Jenkins Shared Library

This repository contains a reusable Jenkins Shared Library
that automates Docker build and push steps.

## Features
- Clone Git repository
- Build Docker image
- Push image to Docker Hub

Used by application repositories via a minimal Jenkinsfile.
